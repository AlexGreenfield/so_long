## Comando consola

valgrind --track-fds=all ./so_long map1.ber

## Impresion mapa ya parseado

printf("\nmap_og\n");
int	i;
	i = 0;
	while (map->map_array[i])
	{
		printf("%s\n", map->map_array[i]);
		i++;
	}

## Data objetos mapa
ft_printf("c is %d\ne is %d\np is %d\npy is %d\npx is %d\n", map->c, map->e, map->p, map->p_y, map->p_x);

## Older render borders


//int	render_borders(t_map *map, mlx_t *mlx, t_textures *textures)
//{
	//int	x;
	//int	y;

	//y = -1;
	//while (y++ < map->y_size)
	//{
		//x = -1;
		//while (x++ < map->x_size)
		//{
			//if (put_border(y, x, mlx, map, textures) != SUCCESS)
				//return (X_ERROR);

		//}
	//}
	//return (SUCCESS);
//}

//int	put_border(int y, int x, mlx_t *mlx, t_map *map, t_textures *textures)
//{
	//int	i;

	//i = SUCCESS;
	//if (y == 0 && x == 0)
		//i = mlx_image_to_window(mlx, textures->c_top_left_i, (WIDTH * x), (HEIGHT * y));
	//else if (y == 0 && x == map->x_size - 1)
		//i = mlx_image_to_window(mlx, textures->c_top_right_i, (WIDTH * x), (HEIGHT * y));
	//else if (y == map->y_size - 1 && x == 0)
		//i = mlx_image_to_window(mlx, textures->c_bot_left_i, (WIDTH * x), (HEIGHT * y));
	//else if (y == map->y_size - 1 && x == map->x_size - 1)
		//i = mlx_image_to_window(mlx, textures->c_bot_right_i, (WIDTH * x), (HEIGHT * y));
	//else if (y == 0)
		//i = mlx_image_to_window(mlx, textures->b_top_i, (WIDTH * x), (HEIGHT * y));
	//else if (y == map->y_size - 1)
		//i = mlx_image_to_window(mlx, textures->b_bot_i, (WIDTH * x), (HEIGHT * y));
	//else if (x == 0)
		//i = mlx_image_to_window(mlx, textures->b_left_i, (WIDTH * x), (HEIGHT * y));
	//else if (x == map->x_size - 1)
		//i = mlx_image_to_window(mlx, textures->b_right_i, (WIDTH * x), (HEIGHT * y));
	//if (i < 0)
		//return (X_ERROR);
	//return (SUCCESS);
//}