## Comando consola

valgrind --track-fds=all ./so_long map1.ber

## Impresion mapa ya parseado

printf("\nmap_og\n");
int	i;
	i = 0;
	while (map->map_array[i])
	{
		printf("%s\n", map->map_array[i]);
		i++;
	}

## Data objetos mapa
ft_printf("c is %d\ne is %d\np is %d\npy is %d\npx is %d\n", map->c, map->e, map->p, map->p_y, map->p_x);
